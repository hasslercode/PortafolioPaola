/**
 * Atomic Design — organism placeholders.
 * Implementations migrate from `_legacy` in Phase 2.
 */

export { SiteHeader } from '@/components/layout/SiteHeader';
export { SiteFooter } from '@/components/layout/SiteFooter';
export { SchemaProvider } from '@/components/seo/SchemaProvider';
