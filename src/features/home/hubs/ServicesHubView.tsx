'use client';

/**
 * Servicios hub — offerings live in PricingHubView on the same page.
 * Kept as a thin export so imports stay stable if we add a short intro later.
 */
export function ServicesHubView() {
  return null;
}
